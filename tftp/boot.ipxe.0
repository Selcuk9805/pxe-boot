#!ipxe
dhcp || dhcp net0 || ifopen net0 || goto failed
chain http://10.30.1.20/boot.ipxe || goto failed

:failed
echo [HATA] Ag hazir degil veya HTTP erisimi yok.
echo [HATA] net0/ip=${net0/ip}  net0/mac=${net0/mac}
sleep 3
shell
